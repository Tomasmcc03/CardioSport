
  # BIT Prototype

  This is a code bundle for BIT Prototype. The original project is available at https://www.figma.com/design/w3KPugo8upgQjx28H2xkKr/BIT-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  